# Setup with public IP? not needed; link to Load Balancer's Backend Pool
# Has a health node point in TCP to be conected to the Azure Load Balancer?
# Be the first 2 Nomad nodes? In counts of a pair?

# It looks like a normal Nomad node; but with the native running
# Uses systemd to ensure it is up
# Use module to know what are the list of available nodes to choose from?
# Select them based on ..? Dedicated resources?